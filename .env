# dotenv-expand is unsupported.
# Do not set NODE_ENV here — Vite sets it for dev/build; .env only allows NODE_ENV=development.

WEB_DROP_BASE_URL=
WEB_DROP_PEER_HOSTNAME=/
# Should has same prefix with WEB_DROP_BASE_URL
WEB_DROP_PEER_PATH=/peer
